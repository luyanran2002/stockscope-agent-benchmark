# Optional: enables PE / valuation data (free Alpha Vantage key)
ALPHAVANTAGE_API_KEY = "your_key_here"

# Optional: enables recent public X posts using the official X API.
X_BEARER_TOKEN = "your_x_bearer_token_here"
